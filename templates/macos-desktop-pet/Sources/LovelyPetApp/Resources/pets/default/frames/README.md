# Frame Resources

Add production transparent PNG frames here before packaging a paid build.

The source template intentionally starts with empty frame arrays so `swift build` works before real animation assets are injected.
