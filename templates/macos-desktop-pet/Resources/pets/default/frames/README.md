# Animation Frames

Place transparent PNG frame sequences here:

```text
frames/
  idle/0001.png
  idle/0002.png
  hover/0001.png
  tap/0001.png
```

The MVP manifest references placeholder paths. Add real transparent PNG frames before running the validator in strict production mode.
