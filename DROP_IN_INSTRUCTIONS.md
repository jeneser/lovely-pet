# Drop-in usage

Unzip this archive at the root of `jeneser/lovely-pet`.

```bash
unzip lovely-pet-cat-assets-sync.zip -d .
make validate
make run
```

The package replaces the default pet image UI assets with transparent PNG frames and an updated manifest.
