name: Test write permission
on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/test-write-permission.yml
permissions:
  contents: write
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
