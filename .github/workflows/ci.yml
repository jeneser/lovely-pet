name: CI

on:
  push:
  pull_request:

jobs:
  macos-template:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Validate manifest
        run: make validate
      - name: Build Swift template
        run: make build
      - name: Package app bundle
        run: make package
