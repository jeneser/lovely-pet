name: Test PR trigger
on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/test-pull-request-trigger.yml
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
